export const Store = {
  async get(key) {
    // placeholder – KV/D1 will be connected later
    return null;
  },

  async set(key, value) {
    // placeholder
    return true;
  },

  async delete(key) {
    // placeholder
    return true;
  },

  async list(prefix = "") {
    // placeholder
    return [];
  }
};
